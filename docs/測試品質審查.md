## 測試品質審查報告

> 審查日期：2026-03-29 | 審查範圍：~/projects/nova-brain/tests/ | 審查者：reviewer(opus)

### 統計
- 總測試檔案：57
- 總測試案例：1338
- 可疑測試：9
- 有效率：99.3%

---

### 可疑測試

#### 虛假測試（永遠會過）

| 檔案:行數 | 測試名稱 | 問題 | 嚴重度 | 建議 |
|-----------|---------|------|:------:|------|
| `architecture.test.js:150` | `有效碼 <= 800 行` | `expect(true).toBe(true)` — 無論行數多少永遠通過 | 🔴 | 改為 `expect(effective).toBeLessThanOrEqual(800)` |
| `hook-client.test.js:62-71` | `nova-server 連線測試` | `typeof connected === 'boolean'` 永遠為真 | 🟡 | 移除或改為驗證 health response 結構 |
| `tool-registry-cli.test.js:39-48` | `scan 命令輸出結果` | `typeof output === 'string'` 永遠為真 | 🟡 | 改為驗證 output 含預期關鍵字 |

#### 無效測試（靜默跳過）

| 檔案:行數 | 測試名稱 | 問題 | 嚴重度 | 建議 |
|-----------|---------|------|:------:|------|
| `judge.test.js:183-209` | `disallowedTools 防護` | `if (!realSkillExists) return;` 靜默跳過無斷言 | 🟡 | 改用 `test.skipIf` |
| `tool-matcher-cli.test.js:26-27` | `match 命令執行` | `if (proc.pid === undefined) return;` 靜默跳過 | 🟡 | 改用 `test.skipIf` |

#### 過時測試

| 檔案:行數 | 測試名稱 | 問題 | 嚴重度 | 建議 |
|-----------|---------|------|:------:|------|
| `nova-server-e2e.test.js:12` | 整個 E2E describe | server 未啟動時 `describe.skip`（合理但 CI 應覆蓋） | 🟢 | CI 確保 server 啟動 |

#### 重複測試

| 檔案:行數 | 測試名稱 | 問題 | 嚴重度 | 建議 |
|-----------|---------|------|:------:|------|
| `task-adapter.test.js:385,548,584` | `planForTask fallback` | 三個 describe 中高度重複 | 🟢 | 合併為一個 test case |
| `hook-client.test.js:9,79` | `FALLBACK_MODULES` | 同檔案定義兩份相同常數 | 🟢 | 合併為單一常數 |

---

### 總體評估

測試套件品質很高（99.3% 有效率）。9 個可疑項中真正有害的只有 1 個（architecture.test.js 的虛假斷言）。主要問題是弱斷言和靜默跳過的可見性。
