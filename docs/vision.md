# Overtone Vision — 通用自主代理核心

> **核心夠通用，外層自動生成。**
> **給它一個目標，它自己建構出整個專業能力。**

## 終極目標

Overtone 不是一個開發工具，而是 **AI 的作業系統核心**（kernel）——一個完全通用的自治大腦，能在收到任何領域的目標後，自動收集知識、建立專業 skill 和 agent、完成任務並持續優化。

核心本身不包含任何領域知識，它提供的是**框架**：守衛框架、評分框架、學習框架、收斂框架。領域知識和專業能力是核心在運作過程中**自動延伸生成**的。

## 五層同心圓架構

```
        ┌────────────────── Layer 5：產品 ──────────────────┐
        │  ┌──────────── Layer 4：通用代理人 ────────────┐  │
        │  │    ┌──────── Layer 3：自我進化 ────────┐    │  │
        │  │    │    ┌── Layer 2：感知操控 ──┐      │    │  │
        │  │    │    │   ┌─ Layer 1 ─┐      │      │    │  │
        │  │    │    │   │           │      │      │    │  │
        │  │    │    │   │  🧠 核心   │      │      │    │  │
        │  │    │    │   │  通用大腦  │      │      │    │  │
        │  │    │    │   │           │      │      │    │  │
        │  │    │    │   └───────────┘      │      │    │  │
        │  │    │    │   手/眼/耳/嘴        │      │    │  │
        │  │    │    └──────────────────────┘      │    │  │
        │  │    │    Gap偵測/Skill Forge/自主建構    │    │  │
        │  │    └──────────────────────────────────┘    │  │
        │  │    AI 本身：通用介面 + 直接操控電腦          │  │
        │  └────────────────────────────────────────────┘  │
        │    產品/專業代理人（開放集合，幾乎什麼都能做）          │
        └──────────────────────────────────────────────────┘
```

### Layer 1：核心大腦（通用型）← Overtone 當前階段

完全通用的自治引擎。不含任何領域知識，只提供框架和機制。

| 框架 | 職責 | 現有實現 |
|------|------|---------|
| 守衛框架 | 定義和執行安全邊界 | hooks + guard-system.js |
| 學習框架 | 觀察 → 記憶 → 改進 | Instinct + Knowledge Engine（11 domains） |
| 評分框架 | 多維度品質評估（不只 pass/fail） | 部分（需擴展為數值制） |
| 收斂框架 | 識別冗餘、做減法 | 部分（需新增） |
| 回饋迴路 | 接收外部信號並調整行為 | 部分（需通用化） |
| 任務引擎 | 分解、排程、委派、追蹤 | workflow + Loop + agent delegation |
| 進化引擎 | 自主建立新 skill / agent | 雛形（gap detection + manage-component） |

### Layer 2：感知與操控（五官四肢）

讓核心能精準操控電腦和感知世界的通用能力。一個能力可被所有領域共用。

**實現架構**：Bun 腳本庫（`scripts/os/`）——agent 透過 Bash tool 呼叫，輕量且可測試。
**知識注入**：`os-control` knowledge domain skill（第 12 個）——讓 agent 知道有哪些 OS 能力、何時用、怎麼用。
**桌面操控**：AppleScript/JXA 原生優先（快準）+ Computer Use 截圖→理解→操作→驗證兜底（通用）。

| 能力 | 說明 | 實現 | 現況 |
|------|------|------|:----:|
| 檔案系統 | 讀寫檔案、目錄操作 | Claude Code 原生 | ✅ |
| 終端操控 | 執行命令、讀取輸出 | Claude Code 原生 | ✅ |
| HTTP/API | 呼叫任意 REST API | Claude Code 原生 | ✅ |
| 瀏覽器操控 | 自主瀏覽、表單填寫、資料擷取 | Chrome MCP | 🟡 |
| 截圖 | 全螢幕/視窗/區域截圖 | `screencapture` | ✅ |
| 視覺理解 | 螢幕截圖 → 結構化理解 | 截圖 + Claude 多模態 | 🟡 |
| 鍵盤模擬 | 按鍵/快捷鍵/文字輸入 | `osascript` System Events | ⬜ |
| 滑鼠模擬 | 點擊/拖曳/滾動 | `cliclick` | ⬜ |
| AppleScript | macOS app 原生操控 | `osascript -l JavaScript` | ⬜ |
| Computer Use | 截圖→理解→操作→驗證迴圈 | 組合能力 | ⬜ |
| 視窗管理 | 列表/聚焦/移動/調整大小 | AppleScript + Accessibility | ✅ |
| Process 管理 | 列出/啟動/終止 process | `ps`/`kill`/`open` | ✅ |
| 剪貼簿 | 讀/寫剪貼簿 | `pbcopy`/`pbpaste` | ✅ |
| 系統資訊 | CPU/記憶體/磁碟/網路 | `sysctl`/`vm_stat` | ✅ |
| macOS 通知 | 系統通知推送 | `osascript` display notification | ✅ |
| 檔案監控 | 檔案系統變更偵測 | `fswatch` | ✅ |
| WebSocket | 即時雙向通訊 | Bun 原生 WebSocket API | ✅ |
| 文字轉語音 | TTS | macOS `say` | ⬜ |
| 語音轉文字 | STT | macOS Dictation / Whisper | ⬜ |
| OS Guard | OS 操作安全邊界 | guard-system 擴展 | ⬜ |

### Layer 3：自我進化（質變層）

核心獲得自我改造能力。面對未知需求時，能自主偵測缺口、建立新 skill/agent/workflow，而非依賴人工擴充。

| 能力 | 說明 |
|------|------|
| Gap 偵測與修復 | 自動偵測元件缺口並修補 |
| Skill Forge | 從零建立不存在的 skill |
| 深度需求收集 | PM 級別的自主需求分析 |
| 自主建構引擎 | 收到高層目標，自主迭代開發 |
| 經驗內化 | 專案經驗轉化為永久能力（飛輪） |

### Layer 4：通用代理人（AI 本身）

Layer 4 就是 AI 本身——一個通用代理人介面。可以用任何形式與使用者互動（CLI 對話、語音、參加會議），也能直接操控電腦完成任務。可以想像成一個**超級智慧家居的終極進化版**：不只控制家電，而是理解意圖、自主行動、跨領域完成任務。

**核心職責**：接收 Layer 5 的需求，運用 L1-3 的能力來完成。

**能力屬性**：

| 能力 | 說明 | 範例 |
|------|------|------|
| 任意互動 | 用任何形式溝通，辨識對話對象與語境 | 聊天、語音、進 Discord 群聊（辨別誰在跟你說話） |
| 直接操控 | 透過 L2 感知操控能力直接控制電腦 | 調音量、開關麥克風、開啟應用程式 |
| 跨領域自主建構 | 收到新領域目標，自主建立 skill/agent/workflow | 分析股票走勢、建立社群帳號 |
| 場景泛化 | 多個不相關領域同時穩定運作 | 同時管理開發專案 + 研究投資 + 回覆客服 |
| 經驗遷移 | 從 A 領域學到的模式加速 B 領域建構 | 開發中學到的 BDD 模式用於交易策略驗證 |

### Layer 5：產品（開放集合）

Layer 5 是 Layer 4 的產出——使用者面向的最終產品或專業代理人。L5 是**開放集合**，不限定為特定產品形態，幾乎什麼都能做。

**產出形態範例**（非窮舉）：

| 類型 | 範例 |
|------|------|
| 專業領域客服 | 金融諮詢、醫療問答、法律顧問 |
| 應用程式 | CLI 工具、Web 服務、自動化腳本 |
| 專業代理人 | PM、HR、會計、工程師、交易員 |
| 自動化系統 | 量化交易、企業運維、內容排程 |
| 社群互動 | 社群經營、AI-to-AI 協作 |

### 遞迴架構

五層架構是**遞迴**的，不是線性堆疊：

```
L1-3 = 怎麼做好（框架、知識、進化機制）
L4   = 誰在做（AI 本身，通用代理人介面）
L5   = 做出什麼（產品、專業代理人）
```

**遞迴性**：L5 產品本身可以內嵌 L1-3 架構，形成自治子系統。例如，一個 L5 客服代理人內部可以有自己的守衛框架（L1）、語音感知（L2）、和自學習能力（L3），獨立運作。

## 核心信念

- **核心夠通用 → 外層自動生成**：不需要手動建造每個領域的能力，核心有自我進化能力後會自己做
- **同心圓輻射**：每一圈從內圈自然延伸，不是獨立堆疊
- **內層決定上限**：外層的品質永遠不會超過核心的自治能力
- **沒有固定形態**：系統可以是任何角色、任何團隊、任何產品
- **自動收斂**：能擴展也能做減法，識別冗餘並移除
- **穩定性 > 功能數量**：一個可靠的能力比十個不穩定的功能更有價值

### 製作原則（品質守衛）

核心信念在實作層面由三條製作原則具體化，確保每個元件都達到同等品質標準：

1. **完全閉環**：每個流程必須有回饋路徑，資訊不能只流出不流回
2. **自動修復**：每個元件必須定義失敗恢復策略，crash 不能只靠人工
3. **補全能力**：系統主動偵測缺口並建議補全，不只報告，要建議行動

> 完整製作原則、可檢驗標準、現有實踐與缺口分析：`docs/spec/overtone-製作規範.md`

## 設計原則

### 1. Skill 化（模組化）

```
每一個能力 = 獨立的 skill / API / script
而不是一段嵌在 hook 裡的邏輯
```

- 所有核心邏輯可被獨立呼叫和測試
- 系統能「看到自己的能力清單」→ 判斷缺什麼 → 自己建立
- Unix 哲學：小而專，可組合

### 2. 確定性與模糊性分離

```
確定性邏輯 → JavaScript（控制流、守衛、狀態管理）
模糊判斷   → LLM（理解意圖、生成內容、做決策）
```

- 核心行為不依賴 LLM 的推理品質
- 無論底層 LLM 是什麼，控制流都可預測
- 未來遷移到本地 LLM 的前提

### 3. 自動收斂

```
成長 = 加功能
成熟 = 加 + 減 + 優化
```

- 每個功能有使用率追蹤
- 未被使用的能力自動識別並建議移除
- 收斂需要明確的目標函數

### 4. 框架優於實例

```
核心提供「守衛框架」→ 領域用它建「風控守衛」
核心提供「評分框架」→ 領域用它定義「策略勝率評分」
核心提供「回饋框架」→ 領域用它接「市場數據回饋」
```

- 核心永遠不包含領域知識
- 核心提供的是建構領域能力的工具
- 這是通用性的保證

### 5. Agent 專一化 × Model 經濟性

```
Agent 越專一 → 任務越簡單 → 可用更小的 model → 成本降低 + 品質不變
知識在 Skill 裡 → 不在 Model 推理能力裡 → Skill 越完善，Model 降級越安全
```

**核心邏輯鏈**：

- **專一化驅動降級**：一個只負責「程式碼風格檢查」的 agent 不需要 Opus 級別的推理能力；一個擁有完整 RCA 框架 skill 的 debugger 可以「查表」而非「推理」
- **知識外化是關鍵**：把知識從「依賴 model 推理品質」轉移到「結構化 skill reference」，是安全降級的前提
- **集中式協調是安全拓撲**：Main Agent 統一調度，agent 之間不直接互動，避免 Google/MIT 研究揭示的「17x 錯誤放大」陷阱

**業界佐證**（2024-2026）：

| 來源 | 結論 |
|------|------|
| RouteLLM (LMSYS, ICLR 2025) | 動態 model routing 成本節省 45-85% |
| NVIDIA | 專職小模型營運成本低 10-30x |
| Google/MIT (arXiv:2512.08296) | 集中式協調錯誤放大 4.4x vs 分散式 17.2x |
| Micro-Agent 設計運動 | 職責清楚 → 行為可預測、受約束、高效率 |

**Overtone 三層模型分配**：

| 層級 | 模型 | 條件 | 目的 |
|------|------|------|------|
| 決策層 | Opus | 需語義判斷、跨 session 記憶、信心門檻 | 品質底線 |
| 執行層 | Sonnet | 有明確 spec、步驟清晰、skill 提供知識 | 效率 × 成本 |
| 協作層 | Haiku | 純文本處理、評分、無推理需求 | 極致成本 |

**設計約束**：Agent 增加 ≠ 盲目膨脹。每個新 agent 需要佐證其必要性（任務無法被現有 agent 處理 + 拆分後可降級 model）。

### 6. 流水線即透明度

```
Pipeline = 品質控制機制 + 使用者可見性
同一套流水線同時服務兩個目的
```

- 使用者需要的是「目前在做什麼、進度如何」，不是「每個 token 的內部推理」
- 透明度是信任的關鍵中介（McKinsey 2024：40% 受訪者認為可解釋性是 AI 採用的關鍵風險）
- 但過度揭露反而降低信任 — 找到正確的抽象層級
- Overtone 的 Dashboard + Status Line + Timeline 已實現三層可見性

### 7. 平台策略

```
當前：Claude Code CLI 作為 runtime（最強 LLM + 最完整平台功能）
中期：本地 LLM + Claude Code CLI（替換模型，保留 runtime）
終極：核心邏輯可脫離 Claude Code（不急）
```

## 從 OpenClaw 學到的教訓

OpenClaw（247K GitHub stars）是 2026 年最大的 AI agent 專案，但暴露了嚴重問題：

| 教訓 | Overtone 的應對 |
|------|----------------|
| 20% 惡意 skills → 供應鏈攻擊 | Skill 由核心自己創建，不依賴外部市集 |
| 「回報成功但實際失敗」 | 品質門檻 + BDD 驗收 + 多層守衛 |
| 黑盒問題（不知道 AI 在做什麼） | Dashboard + Status Line + Timeline 即時可見 |
| 成本失控（$200/天） | Token 追蹤 + 未來預算上限機制 |
| 430K 行 → 攻擊面大 | 保持精簡，做減法比做加法更重要 |

**核心差異**：OpenClaw 追求廣度（50+ 整合、3,286 skills），Overtone 追求深度（可靠、自治、自我進化）。可靠性是最終的護城河。

## 內層成熟度模型

### Level 1：穩定的自治 ← 核心完成

> 「給一個任務，能全自動完成並交付品質合格的結果」

| 能力 | 狀態 |
|------|:----:|
| 任務全自動推進（PLAN → DOCS） | ✅ |
| 過程中自動修復錯誤 | ✅ |
| 品質門檻自動強制（test + lint） | ✅ |
| Skill 化重構（hook → 獨立模組） | ✅ |

**完成標準**：Overtone 核心自治能力已驗證（15 次 Phase 0 任務 + 系統強化 P1-P4 完成）。外部專案驗證已取消（改為在 Level 2 中自然驗證）。

### Level 2：持續學習 ✅ 完成

> 「不只完成任務，還能從每次執行中變得更好」

| 能力 | 狀態 |
|------|:----:|
| 跨 session 長期記憶 | ✅ |
| 數值評分引擎（通用的多維度評估） | ✅ |
| 即時回饋迴路引擎（接收外部信號） | ✅ |
| 時間序列學習（Pattern 效果隨時間的變化） | ✅ |
| 自動識別重複卡點並改進 | ✅ |
| 學習衰減（過時知識自動淡化） | ✅ |
| 效能基線追蹤 | ✅ |

**完成標準**：系統能展示「第 10 次做同類任務比第 1 次更快更好」的量化數據。

### Level 3：自我進化 ← 通往通用性的關鍵

> 「收到一個新領域的目標，能自主建構出整個專業能力堆疊」

| 能力 | 狀態 |
|------|:----:|
| 自主偵測知識缺口 → 建立新 skill | ⬜ |
| 自主建立新的專業 agent | ⬜ |
| 自主修改 workflow 模板 | ⬜ |
| 做減法（移除低效/未使用的能力） | ⬜ |
| 自我修改有安全邊界 | ⬜ |

**完成標準**（Acid Test）：給系統一個高層目標（如 `md-blog` CLI 工具），系統自主完成深度訪談 → skill 建構 → 無限迭代 → 產品交付，無需人工編寫 skill 或 agent。

### Level 4：場景泛化

> 「在多個不相關的領域穩定運作」

| 能力 | 狀態 |
|------|:----:|
| 至少 3 個非開發領域成功運作 | ⬜ |
| 跨領域知識不互相干擾 | ⬜ |
| 新領域從零到穩定運作 < 7 天 | ⬜ |

**完成標準**：交易、企業、AI 通訊三個領域各穩定運作 30 天。

## 初期驗證場景（Layer 5 範例）

Layer 5 是開放集合，以下為初期驗證場景，用於壓力測試核心能力：

| 優先序 | 場景 | 為什麼這個順序 |
|:------:|------|--------------|
| 1️⃣ | 自動交易（多市場） | 完全客觀的回饋信號（盈虧），最好的壓力測試 |
| 2️⃣ | 企業自動化員工 | 最接近現有能力的外推 |
| 3️⃣ | AI 間交流 | 最前沿的探索方向 |
| 4️⃣ | 自主社群經營 | 品質判斷最主觀 |

**定位**：個人使用（Product Owner 是第一個也是最大的使用者），先 dogfooding 驗證核心能力。

> 具體 Roadmap 和技術路線見 `docs/roadmap.md`

---

*建立日期：2026-03-03*
*最後更新：2026-03-06*
*基於：PM Discovery Session — 產品終極目標探索 + L4/L5 架構釐清（三次 session）*
