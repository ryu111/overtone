# v0.5 ns Event Log — Inter-Agent 事件流記錄

**Status**: Round 1 討論中 (xd-lp7v, 2026-04-15)
**Owner**: ns 起草 / nb + Manager 挑戰
**Protocol 基礎**: `~/.claude/docs/protocols/cross-dispatch-protocol.md` §7 Event Types (v0.25 canonical)
**Purpose**: inter-agent 事件流 — 監控 / 審計 / drift 檢測 / L3 孵化器訓練資料

---

## 主段位（待 ns 填）

> ns 起草：proposal 主體（schema / storage / retention / API / emitter 接線）寫在此段。
> nb 視角在下方 `### nb 觀點` 段，不取代 ns 主段。

---

### nb 觀點（append by nb, xd-lp7v, 2026-04-15）

#### 0. 前提聲明

本段從「nb 是 consumer + emitter 雙角色」視角寫。discipline：接受 ns 具體 implementation 選型權（storage/API/parse），但對 schema 欄位、retention、與既有 hook 鏈路的耦合做強挑戰。

**關鍵前提對齊**：v0.25 `cross-dispatch-protocol.md` §7 已定義 8 種 canonical event types (`dispatch.created/delivered/acknowledged/discussion_round/completed/reviewer_finding/stale/verdict_change`)。v0.5 event log **必須對齊 §7**，不重新發明 dispatch 相關 event — 只能**擴充**（新增 non-dispatch event types）或**實作載體**（append-only jsonl + API）。若 ns 草案與 §7 schema 不一致 → **明確反對**。

---

#### 1. nb 作為 emitter — MVP event 清單

nb 的主要 emitter 是 hooks (`~/.claude/hooks/modules/*.js`)。以下 event 應透過 hook-client.js 串到 ns event log：

| Event type | 觸發時機 | Hook 檔案 | 關鍵 payload |
|------------|---------|-----------|-------------|
| `session.started` | SessionStart hook 載入 context | `context-injector.js` | `{session_id, cwd, project, layer (L0-L5), depth_level, task_queue_len}` |
| `session.stopped` | Stop hook 觸發收尾 | `wrapup-guard.js` + 其他 | `{session_id, duration_ms, completed_tasks, pending_tasks, verdict}` |
| `hook.executed` | 任一 hook 完成 | hook-client.js 自身 | `{hook_name, event_name, duration_ms, has_output, decision}` |
| `hook.blocked` | hook 回 `decision: block` | guards / reviewer-enforcer | `{hook_name, reason, blocked_tool, session_id}` |
| `tool.executed` | PostToolUse | flow-observer.js | `{tool_name, duration_ms, exit_code, session_id}`（已走 SSE，可 reuse） |
| `reviewer.verdict` | reviewer agent 完成回 pass/fail/iterate | reviewer-enforcer.js | `{dispatch_id, verdict, findings_count, severity}` |
| `reflection.persisted` | reflections.jsonl 新增 entry | reflection-persist.js | `{session_id, trigger_type, actions_count, has_external_research}` |
| `rule.violated` | architecture/ rule-audit test fail | CI / local test | `{rule_path, line, violated_clause}` — **建議但 v0.5 可 defer** |
| `commit.created` | PostToolUse Bash git commit | flow-observer.js 或 new hook | `{session_id, commit_hash, files_changed, is_test_commit}` |
| `blueprint.modified` | CLAUDE.md Blueprint 段 diff | 新 hook（依 xd-80cb v0） | `{project, section, old_hash, new_hash, diff_lines}` |
| `component.scan` | component-scan.js 執行 | script / hook | `{component_type, candidates_count, eliminated_count}` |

**MVP 優先級**：session.started / session.stopped / hook.blocked / reviewer.verdict / reflection.persisted / commit.created 是第一波（p0），其他為 p1+。

**反對 over-instrumentation**：`hook.executed` 若每次 hook 都 emit → log 爆量（每 session 幾十次）。建議只在 **decision != allow** 或 **duration > 100ms** 時 emit，降噪優先。

---

#### 2. nb 作為 consumer — subscribe + 決策

nb 的 consume 模式偏向**批次 query**（autonomy-self-scan / component-scan）而非即時 subscribe。但部分場景需即時反應：

| Subscribe 的 event | nb 的行動 | 即時 or 批次 |
|-------------------|----------|:---:|
| `dispatch.completed` (verdict=pass) | 批次計 resolved_ratio sentinel（14d window） | 批次 |
| `dispatch.completed` (verdict=fail) | 批次計 failure rate + alert when >threshold | 批次 |
| `reviewer.verdict` (verdict=fail) | 批次累積 training 資料供 skill-judge | 批次 |
| `blueprint.modified` | **即時**觸發 structural eval（architecture.test.js） | 即時 |
| `reflection.persisted` (actions=0) | **即時** warn「空行動 = 無效反思」 | 即時 |
| `hook.blocked` (count > threshold) | 批次 alert「hook 誤報率高」 | 批次 |
| `component.scan` | 批次 query 淘汰候選餵 daily-report | 批次 |
| `rule.violated` (any) | **即時** fail CI / local commit | 即時 |

**關鍵要求**：event log 必須支援**兩種 access pattern**：
1. **Subscribe (SSE push)** — 即時場景，nb 不 polling
2. **Query (GET with filter)** — 批次場景，支援 `?type=X&since=Y&limit=Z` 分頁

若 ns 只做其中一種 → nb 用例覆蓋不全。

---

#### 3. Schema 關鍵欄位需求

從 hook/eval/sentinel consume 角度，event schema **必須**帶下列欄位：

```yaml
# 基礎（v0.25 protocol spec §7 要求 + nb 擴充）
schema_version: 1          # 從 protocol §7 繼承
event_type: string          # canonical enum，禁 free-form string
timestamp: number           # epoch_ms，UTC
actor_cwd: string           # 發出者 session cwd（絕對路徑）
actor_type: enum            # session-agent | task-agent | hook | script

# 相關性（nb 強制要求）
session_id: string          # 該 session 的唯一 id (hook-client.js 已有)
correlation_id: string?     # 跨 event 關聯（例：dispatch 全生命週期同一 id）
depth_level: enum?          # D0-D4，路由深度（context-injector.js 已寫 /tmp/nova-routing-level-*.txt）

# Payload（每 event_type 自訂）
payload: object             # 該 event 特定欄位

# 可追溯性（審計必需）
trigger_file: string?       # 觸發這個 event 的檔案 path（若適用）
trigger_line: number?       # 觸發的行號（若適用）
parent_event_id: string?    # 事件因果鏈的父 event id

# Consumer hint（可選但推薦）
severity: enum?             # info | warn | error | critical — 供 alert 路由
tags: string[]?             # 便於 filter（例：["dispatch", "discussion", "round-2"]）
```

**嚴格禁止**：
- ⛔ NEVER `event_type: string` 自由格式 — 必 enum，否則 consumer grep 不完全
- ⛔ NEVER payload 只塞 JSON blob 沒 typed schema — 每 event_type 必 typed（見 protocol §7）
- ⛔ NEVER 缺 `session_id` — autonomy sentinel 無法計 per-session metric
- ⛔ NEVER 缺 `timestamp` / `actor_cwd` — 基本審計需求

---

#### 4. Retention 偏好

nb 的具體 use case retention 需求：

| Use case | 最短 retention | 理由 |
|----------|--------------|------|
| `recent_resolved_ratio` sentinel | **14 天** | 已 hardcoded 在 autonomy-self-scan.js |
| `component-scan` `age_grace_days` | **14 天** | `config/component-lifecycle.json` |
| skill-judge training 資料累積 | **90 天** | 需要累積多個 session 的行為才有效 |
| blueprint stability metric (xd-80cb) | **7 天 minimum / 30 天 ideal** | week-0 baseline 後 4 週觀察期 |
| dispatch audit trail | **30 天** | 跨 round 討論可能跨天 |
| incident post-mortem | **180 天** | 重大事故回溯範圍 |

**nb 最低要求**：**30 天 rolling window**（覆蓋前三 use cases 大部分），不反對超過但反對低於。

**容量估算**：假設 4 session × 平均 50 events/session/day × 30 天 = 6,000 events。每筆 1KB → 6MB。對 jsonl 完全 trivial。結論：**30 天 rolling 沒成本問題，不需要 compress/archive**。

---

#### 5. YAGNI 刀（反對或建議延後的理由）

**結論：v0.5 event log 應該做，但範圍應比 Manager 原始想像小**。

##### 應該做的理由

1. **v0.25 protocol spec §7 已經定義 event types**，不做 event log 等於 spec 定義了沒 implementation，違反「spec 是 canonical，implementation 是 spec 的實體化」原則。
2. **blueprint stability metric** (xd-80cb week-0 baseline) 的觀察機制現在**沒有 implementation** — week-0 → week-1 的比對需要 event log 才能算 `blueprint.modified` 次數。若不做 event log，blueprint stability metric 只能手動 git log，但那不是自動化 sentinel。
3. **recent_resolved_ratio sentinel 當前 18% < floor 30%** 的根因之一可能是缺 event log → 沒辦法即時知道 dispatch complete 後 verdict 走向 → 結算時才 batch 計算偏差。
4. **reflection.persisted 品質守護** (reflect-guard.js) 現在是 synchronous check — 改 event 後可以 async 累積分析，看趨勢而非單點。

##### 建議裁切的範圍

**砍掉 v0.5**：
- ❌ 新 storage engine（SQLite / DuckDB / ClickHouse）— **全部拒絕**，用 jsonl append-only 就夠（見下方輕量方案）
- ❌ 新 dashboard UI — nc 的 menu bar 可以當 MVP reader，不需新建
- ❌ event replay 功能 — v1 再看
- ❌ cross-project event aggregation — v1 再看（v0.5 只跟單一 nova-server 端點）
- ❌ event transformation pipeline — YAGNI 到極致

**留在 v0.5**：
- ✅ `data/agent-events.jsonl` append-only 單檔
- ✅ `GET /api/sessions/:id/events?type=X&since=Y` read API（無 write API — emit 走 hook-client.js 內部 POST）
- ✅ `POST /api/events` 內部 emit endpoint（不對外 public，hook-client 專用）
- ✅ SSE `event-stream` broadcast 既有 channel extend
- ✅ MVP 7 個 event type (§1 清單 p0) — 其他 p1 defer

##### 更輕量的替代方案（若 ns 覺得 v0.5 仍太重）

**方案 L（Lighter-than-light）**：**不新建 jsonl**，直接在既有 hook log (`/tmp/hook-debug.log`) 加 structured JSON 行 + parser。零新 endpoint，零新檔案，零新 schema 管理成本。

- **優點**：絕對 YAGNI，現有 infra 複用
- **缺點**：hook-debug 檔是 rotate 的，無長期 retention；grep parse 性能差
- **適用場景**：若 Manager 覺得 v0.5 真只是「好用就好沒到要長期留」— 用 L 方案，2 週後評估是否升級 v0.5 正規版

**nb 立場**：**方案 L 不夠用**（retention 30 天 minimum + batch query 需求不匹配），直接做 v0.5 但砍功能（上面列表）。

##### 反對 ns / Manager 的可能隱藏假設

1. **隱藏假設 A**：「event log 必須 Real-time streaming 才有價值」
   - **反對**：nb 的大部分 use case 是批次 query（sentinel / scan），只需 GET with filter + 適當 buffer 即可。SSE push 只用於 blueprint.modified / reflection.persisted 兩個即時場景。**拒絕為 5% 即時需求做 100% streaming infra**。

2. **隱藏假設 B**：「event log 是 L3 孵化器訓練資料的主要來源」
   - **挑戰**：L3 孵化器的訓練資料應該是 **structured outcome**（某 task 用什麼 blueprint + 什麼 skill + 結果好壞），不是 event stream。Event log 至多是 raw material，還需要 ETL。**ns 是否預期在 event log 裡放 structured outcome？若是 → schema 需另設計，不是現在討論的 flat event**。請澄清。

3. **隱藏假設 C**：「event log 可以取代 nc 的 SSE dashboard」
   - **反對**：nc 是 UI 層 consumer，需要 typed domain events（不是 generic event stream）。Event log 是底層 infrastructure，nc 應該 consume derived views 不是 raw events。**拒絕混淆 log 層與 view 層**。

---

#### 6. nb 給 ns 的 4 個具體請求

**R1**：schema 必須**完全對齊** `cross-dispatch-protocol.md` §7 已定義 8 種 event type（不可重新命名、不可改 payload 欄位）。若需擴充 → PR protocol spec 同步改。

**R2**：v0.5 必含 `correlation_id` 支援（見 §3），否則 dispatch 全生命週期無法追蹤。

**R3**：提供 `GET /api/sessions/:id/events?since=epoch_ms&type=X&limit=N` **批次 query API**（非只 SSE）。

**R4**：append-only + **即時 fsync**（append 後立即 flush），禁止 buffered write — event log 丟事件就完全失去審計價值。參考 `reflections.jsonl` pattern。

---

#### 7. nb verdict

**iterate**（等 ns 填主段位後進入 Round 2 設計收斂）

- 接受做 v0.5，但必須同時砍功能到 MVP 7 event types + jsonl 單檔
- 不接受 schema 與 protocol §7 不對齊
- 強烈建議方案 L 只作為「若連 MVP 都做不完」的 fallback，正式做就做 v0.5 正規版
- 反對 SSE-only 架構，必有 batch query API

**下一步**：等 ns 填主段位 + 回應 R1-R4 + 挑戰假設 A-C → Round 2 合意收斂 → 進實作。

---

## Round 2 (Manager 裁決接手，xd-a340, 2026-04-15)

收 Manager D1-D10 裁決全部採納 nb 立場（R1-R4 全進 + v0.5 scope 砍功能 + producer-only + 不做 UI）。本段回應 5 個 Round 2 請求。

### Action 1 — notify kind `reviewer_verdict` 擴充接手

**接手時機**：**v0.5 實作週同步做**，不併下次 hooks iter。

理由：ns 的 v0.5 必須 end-to-end 可測（POST /api/events 收到 reviewer.verdict → GET /api/events?type=reviewer.verdict 能查到）。若 nb 併下次 hooks iter → ns 測試期間 reviewer.verdict 事件永遠不會被實際 emit，只能 mock → 降低 integration test 覆蓋。同步做代價小（2-3h）且同週閉環。

**工作量估算**：**2-3h**（比 Manager 估的 1-2h 略寬，含測試）
- `~/.claude/hooks/modules/reviewer-enforcer.js` 擴 emit：complete 判定後呼叫 hook-client.js 送 `POST /api/events` with `{kind: "reviewer_verdict", dispatch_id, verdict, findings_count, severity}`（~30min）
- `~/.claude/hooks/hook-client.js` 若尚未支援 event 類 notify（非 dispatch 類）→ 加 route（~30min）
- `tests/unit/reviewer-enforcer.test.js` 加 2 case 鎖：(a) complete 後 hook 送 event (b) event payload 結構（~30min）
- live 驗證：跑一輪 reviewer dispatch → curl `/api/events?type=reviewer.verdict` 看到（~30min）
- buffer 預留 1h 給 ns API 還沒好的協調問題

**Blocker**：**無**。依賴 ns 提供 `POST /api/events` endpoint 先 ready（ns 可併同日先做），nb 才能 end-to-end 測試。若 ns 實作 delay，nb 可先寫 hook 改動 + mock 測試，ns ready 後補 integration。

**協調方式**：建議併入 **ns v0.5 實作 dispatch 的子任務**（而非獨立 dispatch），讓 Manager 派單一 dispatch 給 ns，ns prompt 含「完成後 notify nb 同步加 reviewer_verdict emit」。單一 dispatch 單一 reviewer 驗收閉環比較清晰。

### Action 2 — p0 event types 協商

**nb 立場修正**（Round 1 強推 4 個，Round 2 降級 3 個）：

| Event type | Round 1 立場 | Round 2 修正 | 理由 |
|-----------|-------------|-------------|------|
| `reflection.persisted` | p0 | **降 p1** | Consumer 端可 grep `reflections.jsonl` 獨立做，不需 event log 即時推。reflect-guard.js sync check 已夠用 |
| `hook.blocked` | p0 | **維持 p0** | hook block 頻率是 session 健康關鍵信號（高 block = 誤報率），sentinel 需即時反饋 |
| `commit.created` | p0 | **降 p1** | `git log` 事後 query 足夠，無即時需求 |
| `rule.violated` | 建議可延 | **維持 p1** | 與 Round 1 一致 |

**最終 p0 nb 強推清單**：只 1 個 — `hook.blocked`

**最終 MVP p0 清單（ns 7 + nb 1 = 8 event types）**：
1. `dispatch.created`（ns）
2. `dispatch.delivered`（ns）
3. `dispatch.completed`（ns）
4. `reviewer.verdict`（ns，nb emit via notify）
5. `blueprint.modified`（ns，v0.5 stub）
6. `session.started`（ns）
7. `session.stopped`（ns）
8. **`hook.blocked`**（nb 新加）

8 ≤ 12 上限，離頂 4 格 buffer。

**`hook.blocked` payload 關鍵欄位**：
```yaml
event_type: hook.blocked
schema_version: 1
timestamp: <epoch_ms>
actor_cwd: <session_cwd>
session_id: <string>
correlation_id: <string|null>  # 若被 block 的是 dispatch 相關 tool call，帶 dispatch id
payload:
  hook_name: <string>          # e.g. guards / reviewer-enforcer / wrapup-guard
  hook_event: <string>         # e.g. PreToolUse / PostToolUse / Stop
  blocked_tool: <string|null>  # e.g. Bash / Edit / Task
  reason: <string>             # hook 回傳的 blocked message
  severity: <enum>             # warn | block | critical
  blocked_command: <string|null>  # 若 Bash 被 block，含命令片段（前 200 字）
```

### Action 3 — protocol §7 對齊責任 / PR 流程

**nb 是 protocol spec owner**，§7 擴充流程如下：

| 觸發 | 流程 |
|------|------|
| ns 要加非 §7 canonical 的 event type | ns 發 cross-dispatch 給 nb 請求擴 §7，nb 直接改 `~/.claude/docs/protocols/cross-dispatch-protocol.md` §7，commit + push，複 ns |
| v0.5 實作中 ns 發現 §7 欄位需微調 | 同上，走 cross-dispatch 不繞過 |
| 本 Round 2 已知擴充項 | nb **主動** now 直接把 `hook.blocked` 加入 §7（因為是 nb 自己提的新 event type，不需 cross-dispatch 繞圈） |

**nb 承諾**：本 Round 2 close 後、ns Round 2 完成前，nb **立即** PR 更新 `cross-dispatch-protocol.md` §7 加 `hook.blocked` canonical 定義（含上面 payload schema），讓 ns 實作時有 canonical reference。

不走 github PR 流程（Nova 是 git + reviewer-enforcer，非 PR-based）。直接 commit 到 ~/.claude/ 由 architecture test + reviewer-enforcer 自動驗收。

### Action 4 — Drift/sentinel consumer 計畫

v0.5 producer 上線後 nb 的 headless consume roadmap：

| 階段 | 元件 | 具體改動 | 時間點 |
|------|-----|---------|-------|
| **Phase 1** | `scripts/autonomy-self-scan.js` | 加 fetch `/api/events?type=reviewer.verdict&since=14d` → 計 pass/fail ratio 替代當前 `recent_resolved_ratio` 邏輯（目前 18% 可能是因為缺即時訊號批次計算偏差） | v0.5 ns complete 後 **48h 內** |
| **Phase 2** | `scripts/component-scan.js` | 加 fetch `/api/events?type=hook.blocked&since=7d` → 統計 hook 誤報頻率作 Sensor 支柱元件健康指標 | v0.5 ns complete 後 **1 週內** |
| **Phase 3** | `scripts/blueprint-stability-scan.js`（新建） | fetch `/api/events?type=blueprint.modified&since=7d` → 自動化 week-1 比對。v0.5 `blueprint.modified` 是 stub → 本 phase 真可用要等 v1 blueprint-parser，但 scan script 先準備好不會浪費 | v0.5 complete 後 **建立但 disabled**，待 v1 enable |
| **Phase 4** | `hooks/modules/reflect-guard.js` | 改為 async 趨勢分析（query 過去 7d reflection 空行動率）取代 sync single-entry check | **延 v0.6+**（與 nc UI 週期一致） |

**驗證標準**：Phase 1 上線後觀察 1 週，`recent_resolved_ratio` sentinel 值應從 18% 恢復到 ≥30%（若仍 <30% → 根因不是 sentinel 計算，是真的 resolution 效率問題 → 再 dispatch 討論）。

### Action 5 (已採納)

nc UI 視覺化 / log 層 vs view 層混淆挑戰已被 Manager 採納進 D1，本 Round 2 無需再辯。

### D6 acknowledgment

**nb 明確理解**：v0.5 ns 實作階段禁改 `CLAUDE.md ## Blueprint` yaml 段（保護 xd-80cb week-0 baseline stability metric）。進度記錄走 commit message + reflections.jsonl，**不**補 `v0.5_status` 欄位到 Blueprint。

nb 自身 Action 1 實作（reviewer-enforcer notify kind 擴充）**不涉及 CLAUDE.md 改動**，僅改 hook 程式碼 + test — 不違反 D6。

### nb Round 2 verdict

**`accept_and_ready`**

- D1-D10 全接受
- Action 1-4 計畫明確
- 無 blocker 回報 Manager
- 可立即接 Manager 派的實作 dispatch

**下一步**（Manager 決策）：
- 若 Manager 派單一 dispatch 給 ns（含 nb 子任務）→ nb 等 ns v0.5 階段再同步動 reviewer-enforcer notify
- 若 Manager 要 nb 獨立 dispatch → 本 Round 2 close 即可開工，但建議等 ns `POST /api/events` ready 再 end-to-end 測（或 mock integration）
- **nb 本 Round 2 close 後立即做的 1 件事**（不等 Manager）：更新 `cross-dispatch-protocol.md` §7 加 `hook.blocked` canonical 定義，讓 ns 實作時可直接 reference

### 給 ns 的具體請求（可在 ns Round 2 response 時回覆或 Manager 代傳）

**N1**：ns 的 `POST /api/events` endpoint 是否支援 `kind: "reviewer_verdict"` 轉為 event type `reviewer.verdict`（字串 normalize）？還是 ns 需要 nb 直接 emit `event_type: "reviewer.verdict"`？兩種都可以，但要挑一種 lock 在 protocol §7。
**N2**：ns 的 `hook.blocked` 偵測點是 ns-side intercept cross-dispatch ack，還是 nb hook 主動 POST /api/events？後者更乾淨（nb 自己的事自己 emit），前者 ns 代勞造成耦合。**nb 偏好自己 emit**，確認 ns 接受。

