# Instinct 信心分數完整演算法

> 📋 **何時讀取**：分析 instinct 觀察、評估進化候選或除錯信心分數問題時。

## 初始值與變化規則

| 事件 | 分數變化 | 說明 |
|------|:--------:|------|
| 新觀察建立 | `0.30` | 初始信心（剛記錄，尚未驗證） |
| 確認（相同 pattern 再次出現） | `+0.05` | 每次重複出現累加 |
| 矛盾（相反 pattern 出現） | `-0.10` | 單次矛盾的懲罰較大 |
| 週衰減（超過 7 天未更新） | `-0.02` | 每週未使用自動衰減 |

### 信心分數範圍

```
0.0 ────── 0.2 ────── 0.3 ────── 0.7 ────── 1.0
   自動刪除    低信心    初始值    自動應用
```

| 範圍 | 狀態 | 行為 |
|:----:|------|------|
| `< 0.2` | 過時/無效 | 自動刪除 |
| `0.2 - 0.3` | 低信心 | 保留但不應用 |
| `0.3 - 0.65` | 累積中 | 重複確認逐步提升 |
| `>= 0.7` | 高信心 | 可自動應用（無需人工確認） |

## 從初始到自動應用的路徑

```
初始觀察（0.30）
  ↓ +0.05（第 1 次確認）→ 0.35
  ↓ +0.05（第 2 次確認）→ 0.40
  ↓ +0.05（第 3 次確認）→ 0.45
  ↓ +0.05（第 4 次確認）→ 0.50
  ↓ +0.05（第 5 次確認）→ 0.55
  ↓ +0.05（第 6 次確認）→ 0.60
  ↓ +0.05（第 7 次確認）→ 0.65
  ↓ +0.05（第 8 次確認）→ 0.70 ✅ 自動應用門檻
```

**最少需要 8 次確認**才能從初始值到達自動應用門檻。

## 衰減機制

### 觸發條件
- 觀察的 `lastUpdated` 超過 7 天未更新
- 手動執行 `instinct.js decay {sessionId}`

### 衰減計算
```
衰減量 = Math.floor(天數差 / 7) * 0.02
新信心 = 當前信心 - 衰減量
```

### 範例
- 14 天未更新 → `-0.04`（2 週 × 0.02）
- 30 天未更新 → `-0.08`（4 週 × 0.02）
- 初始觀察（0.30）+ 30 天未更新 → `0.22`（仍在保留範圍）
- 初始觀察（0.30）+ 60 天未更新 → `0.14`（自動刪除）

## 進化門檻

### Skill 進化

| 條件 | 值 | 說明 |
|------|:--:|------|
| 同 tag 觀察數 | >= 5 | 同一主題的觀察夠多 |
| 平均信心 | >= 0.7 | 整體信心夠高 |

### Agent 進化

| 條件 | 值 | 說明 |
|------|:--:|------|
| 同 tag 觀察數 | >= 8 | 需要更多觀察（agent 更複雜） |
| 含多步驟 pattern | 是 | 觀察描述涵蓋多步驟流程 |

## 聚類規則

### Tag 聚類
同 `tag` 的觀察自動歸為一組。例：

```
tag: "bun-over-npm"
  - 觀察 1: "使用 bun test 比 npm test 快 3 倍"（0.65）
  - 觀察 2: "bun install 快於 npm install"（0.55）
  - 觀察 3: "bun run build 兼容 npm scripts"（0.70）
```

### 聚類統計
```json
{
  "tag": "bun-over-npm",
  "count": 3,
  "avgConfidence": 0.633,
  "minConfidence": 0.55,
  "maxConfidence": 0.70
}
```

## V1 支援的觀察類型

| 類型 | 說明 | 範例 |
|------|------|------|
| `error_resolutions` | 錯誤和其解決方式 | "MODULE_NOT_FOUND → 需要 bun install" |
| `tool_preferences` | 工具偏好（反面偵測） | "Bash grep/find → 建議改用 Grep/Glob" |
| `agent_performance` | Agent 執行表現 | "developer pass at DEV" |
| `workflow_routing` | 工作流選擇偏好 | "wf-standard — 標準功能" |
| `wording_mismatch` | 措詞不匹配偵測 | "💡 MUST → emoji-關鍵詞不匹配" |

## 設定參考（registry.js）

```javascript
const instinctDefaults = {
  initialConfidence: 0.3,
  confirmBoost: 0.05,
  contradictionPenalty: -0.10,
  weeklyDecay: -0.02,
  autoApplyThreshold: 0.7,
  autoDeleteThreshold: 0.2,
  skillEvolutionCount: 5,
  agentEvolutionCount: 8,
};
```
