{
  "name": "overtone",
  "version": "0.11.0",
  "private": true,
  "description": "Overtone — BDD 驅動工作流自動化 Claude Code plugin",
  "scripts": {
    "server": "bun run scripts/server.js",
    "validate": "bun run scripts/validate-agents.js",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "peerDependencies": {
    "agent-browser": ">=0.1.0"
  }
}
