{
  "name": "overtone-docs",
  "private": true,
  "scripts": {
    "test": "bash scripts/test-parallel.sh --unit",
    "test:all": "bash scripts/test-parallel.sh",
    "test:seq": "bun test tests/unit",
    "test:random": "bun test tests/unit --randomize",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
